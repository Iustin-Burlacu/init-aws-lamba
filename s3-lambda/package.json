{
  "name": "sqs-lambda",
  "version": "0.0.1",
  "description": "lambda triggered by s3",
  "main": "../se-lambda/dist/src/index.js",
  "scripts": {
    "test": "npm run test",
    "compile": "npx rimraf ../sqs-lambda/dist && npx tsc -b",
    "deploy": "deploy.sh"
  },
  "keywords": [
    "aws",
    "s3",
    "lambda"
  ],
  "author": "Iustin Burlacu",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "ts-node": "^10.8.0",
    "ksuid": "^3.0.0",
    "lambda-log": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^4.9.4",
    "@types/node": "^20.4.9",
    "@types/aws-lambda": "^8.10.131",
    "@types/lambda-log": "^3.0.0"
  }
}
